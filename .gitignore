.env
config/credentials.json
config/.token_encryption_key
config/.cache_encryption_key
config/trainerroad_credentials.json
config/.trainerroad_encryption_key
config/secret_key
/data/cache/
/data/weather_cache.json
/data/*.json
/data/*.db
/cache/
/output/
venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.DS_Store
*.log
project_comms/
.mailmap
rewrite_git_history.sh
bob_skills.md
docker-compose.override.yml

# Mock object directories (test artifacts)
<Mock*
<Magic*
config/.trainerroad_encryption_key

# AI Code Assistant internals
.claude/worktrees/

# Maintenance backup archives
backups/maintenance/*.tar.gz

# Backup files — pushed to private backup repo via scripts/backup-env.sh
*.backup
